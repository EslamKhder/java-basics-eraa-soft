package main1;

import main2.Student2;

public class Main1 {

    public static void main(String[] args) {
        /*
        start app create
            Employee(id, name , age)
            Player(id, name , age, th-Number)
            Manager(id, name , address )

                       BaseEntity(id, name)
         *             PublicData(age) : BaseEntity
         *             Employee(): PublicData
         *             Player(th-Number):PublicData
         *             Manager(address ): BaseEntity









            make function for every class
             - printEmployeeData
             - printPlayerData
             - printManagerData
             without repeated any lines
         */

        /**
         * BaseEntity(id, name)
         *             PublicData(age)
         *             Employee(age)
         *             Player(age, th-Number)
         *             Manager(address )
         */
    }
}
